export type TTokenData = {
  id: string;
  nik: string;
  email: string;
  name: string;
  phoneNumber: string;
  role: string;
  photo: null | string;
  address: null | string;
  token: string;
};

export type TToken = {
  token: string;
};
